/*!
    @import /amethyst_input/src/bindings.rs#Bindings, StringBindings
    Bindings<StringBindings>
*/
(
    axes: {
        "zoom": Emulated( pos: MouseWheel(ScrollUp), neg: MouseWheel(ScrollDown)), // this isn't working at the moment
    },
    actions: {
        "help": [[Key(H)]],
        "tonemap": [[Key(F7)]],
        "exposure_decr": [[Key(F8)]],
        "exposure_incr": [[Key(F9)]],
        "fxaa": [[Key(F10)]],
        "debuglines": [[Key(F11)]],
        "fps": [[Key(F12)]],
    },
)